const TextSize = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 18,
  xl: 22,
};

const PaddingSize = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
};

const RoundedSize = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
};

const Height = {
  xs: 30,
  sm: 40,
  md: 60,
  lg: 80,
  xl: 100,
};

export default { TextSize, PaddingSize, RoundedSize, Height };
